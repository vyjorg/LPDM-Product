spring:
  application:
    name: ms-product
  cloud:
    config:
      allow-override: true
      uri: https://config.lpdm.kybox.fr
  jpa:
    hibernate:
      ddl-auto: validate
  main:
    allow-bean-definition-overriding: true